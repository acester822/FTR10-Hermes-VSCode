node_modules/
.vscode-test/
out/
*.vsix
.DS_Store
.chat-history.json
.sessions.json
docs/**
logs/**

# Local config / secrets — never commit
.env
.env.local

# npm only — ignore other package manager lock files
yarn.lock
pnpm-lock.yaml
bun.lock
bun.lockb
# Kanban/worker worktrees (ephemeral, already merged into history)
.worktrees/
